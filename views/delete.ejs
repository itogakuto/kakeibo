<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title>削除確認</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <main class="container">
    <h1>削除確認</h1>
    <article class="card">
      <h3><%= blog.title %></h3>
      <p>投稿日: <%= blog.dateStr %></p>
      <p><%= blog.content %></p>
    </article>

    <form action="/delete/<%= blog.id %>" method="POST" class="actions">
      <a class="btn" href="/">キャンセル</a>
      <button class="btn danger" type="submit">削除する</button>
    </form>
  </main>
</body>
</html>
