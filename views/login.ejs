<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title>家計簿アプリ ログイン</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body class="auth-page">
  <main class="auth-main">
    <div class="auth-card card">
      <div class="auth-logo">家計簿アプリ</div>
      <h1 class="auth-title">ログイン</h1>

      <% if (error) { %>
        <div class="alert">
          <ul>
            <li><%= error %></li>
          </ul>
        </div>
      <% } %>

      <form method="post" action="/login" class="stack">
        <div class="field">
          <span class="field-label">メールアドレス</span>
          <input type="email" name="email" required />
        </div>

        <div class="field">
          <span class="field-label">パスワード</span>
          <input type="password" name="password" required />
        </div>

        <button type="submit" class="btn primary align-right">ログイン</button>
      </form>

      <p class="auth-sub">
        はじめての方は <a href="/register" class="link-primary">新規登録</a>
      </p>
    </div>
  </main>
</body>
</html>
