<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title>投稿の編集</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <main class="container">
    <h1>投稿の編集</h1>

    <% if (errors && errors.length) { %>
      <div class="alert">
        <ul>
          <% errors.forEach(e => { %><li><%= e %></li><% }) %>
        </ul>
      </div>
    <% } %>

    <form action="/update/<%= blog.id %>" method="POST" class="card stack">
      <label>タイトル:
        <input type="text" name="title" value="<%= blog.title %>" />
      </label>
      <label>投稿日:
        <input type="date" name="postedAt" value="<%= blog.dateStr %>" />
      </label>
      <label>内容:
        <textarea name="content" rows="6"><%= blog.content %></textarea>
      </label>
      <div class="actions">
        <a class="btn" href="/">戻る</a>
        <button type="submit">更新</button>
      </div>
    </form>
  </main>
</body>
</html>
